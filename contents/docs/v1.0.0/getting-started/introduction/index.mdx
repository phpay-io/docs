---
title: Introdução
description: O PHPay é uma biblioteca PHP que tem o objetivo tornar o trabalho de integrações com gateways de pagamento mais simples e descomplicadas, facilitando a conexão entre tecnologia e negócios em produtos de software.
---

## Getting Started

Para iniciar você precisa instalar o pacote disponível no packagist.org, para isso siga os passos abaixo:

- Passo 1 - 🏗️ Instalação

```bash
composer require phpay-io/phpay
```

## ⏯️ Uso

<Tabs defaultValue="asaas" className="pt-5 pb-1">
  <TabsList className="">
    <TabsTrigger value="asaas">Asaas</TabsTrigger>
    <TabsTrigger value="efi">Efí</TabsTrigger>
  </TabsList>
  <TabsContent value="asaas">
    ```php showLineNumbers
    /**
    * @var AsaasGateway $phpay
    */
    $phpay = new PHPay(new AsaasGateway(TOKEN_ASAAS_SANDBOX));

    $phpay
        ->charge($charge)
        ->setCustomer($customer)
        ->create();
    ```

  </TabsContent>
  
  <TabsContent value="efi">
    ```php showLineNumbers
        /**
        * @var EfiGateway $phpay
        */
        $phpay = new PHPay(new EfiGateway(CLIENT_ID, CLIENT_SECRET));

        $phpay
            ->charge($charge)
            ->setCustomer($customer)
            ->create();
    ```

  </TabsContent>
</Tabs>

## Conclusion

PHPay simplifies the integration with multiple payment gateways, offering a clean and unified interface to handle various payment operations effortlessly. With just a few steps, developers can set up the library, manage client information, and generate transactions like invoices and QR codes. By leveraging PHPay, you streamline your payment process, saving time and ensuring a more elegant implementation in your projects.
